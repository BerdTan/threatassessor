"""
rag.py - Retrieval Augmented Generation module (stub)
"""
from chatbot.modules import llm

def process(user_input, history, persona, model=None):
    # Future: Retrieve context, then call LLM for response
    # context = retrieve_context(user_input)
    # response = llm.generate_response(f"{context}\n{user_input}", history, persona, model)
    return None
