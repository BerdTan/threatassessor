# Audit rubric — moved

This list now lives in **`references/self-audit.md`**, which supersedes it.

It absorbed everything that was here and adds the parts this version couldn't check: the measured cross-check against `measured.json` written by `shoot.py`, the Phase 0 question about direction, and an explicit evidence-per-line format so a skipped line is visible rather than silent.

Read `self-audit.md`. Don't work from this file.
